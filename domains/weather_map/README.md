# Duplicate domain pack (legacy)

Canonical weather domain pack lives at `domains/weather_map/` in the repo root.

This copy under `packages/uri3/domains/` is retained for backward-compatible imports only.
Do not edit here — change `domains/weather_map/` and regenerate agents via nl2a.
